# Security policy

Do not report suspected vulnerabilities in a public issue when the report contains a working exploit, sensitive data or details that would materially reduce the time needed to abuse the flaw.

When evaluating a report, include the affected version, operating system, minimal reproduction steps and the expected security property that failed.

Cryptographic changes should include tests covering authentication failure, malformed envelopes and backwards compatibility with existing envelope versions.
